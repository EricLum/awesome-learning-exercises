/*
Group Exercise One - Handling multiple promises in parallel

Write a function requestAllFiles that takes an array of promises 
(something like [getFile('file1'), getFile('file2')....])
and returns the return values of all the completed promises in the provided array. 
*/
function requestAllFiles(arrayOfPromises) {
  // Your solution here
}

export { requestAllFiles };
